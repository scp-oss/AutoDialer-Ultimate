# TEMPLATE ONLY — do not apply as-is and do not commit real values.
# Copy to 02-secret.yaml (gitignored), fill in real secrets, and apply that
# file instead. In production prefer a secrets manager integration (e.g.
# External Secrets Operator, Sealed Secrets, or your cloud provider's CSI
# driver) over a plain Secret checked into any repo.
#
# Mirrors the required (":?required") variables from docker-compose.yml /
# .env.example: DB_PASSWORD, JWT_SECRET (32+ chars), AMI_PASSWORD,
# EXTENSION_PASSWORD, FREEPBX_IP (not secret, but required — kept here for
# convenience alongside the credentials it pairs with).
apiVersion: v1
kind: Secret
metadata:
  name: autodialer-secrets
  namespace: autodialer
type: Opaque
stringData:
  DB_PASSWORD: "CHANGE_ME"
  JWT_SECRET: "CHANGE_ME_32_CHARS_MINIMUM_XXXXXXXXXX"
  AMI_PASSWORD: "CHANGE_ME"
  ADMIN_AMI_PASSWORD: "CHANGE_ME"
  MONITOR_AMI_PASSWORD: "CHANGE_ME"
  REDIS_PASSWORD: ""
  METRICS_USER: "admin"
  METRICS_PASS: "CHANGE_ME"
  FREEPBX_IP: "192.168.1.100"
  FREEPBX_EXTENSION: "291"
  EXTENSION_PASSWORD: "CHANGE_ME"
