# NOTE on frontend/dist: docker-compose.yml bind-mounts ./frontend/dist
# into both backend and nginx read-only — there's no host filesystem to
# bind-mount from in Kubernetes. Add `COPY frontend/dist /srv/frontend` to
# Dockerfile (or a separate init step) before building the image referenced
# below, otherwise STATIC_DIR will be empty in the container.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
  namespace: autodialer
spec:
  replicas: 2
  selector:
    matchLabels:
      app: backend
  template:
    metadata:
      labels:
        app: backend
    spec:
      # retry_queue/transcription_queue/health_monitor/metrics are NOT
      # leader-gated, and that's correct by design (see ROADMAP.md §3.8):
      # retry_queue uses SELECT ... FOR UPDATE SKIP LOCKED, transcription
      # actually runs via redis blpop() elsewhere, health/metrics reflect
      # the local replica. Only cleanup_audio/log_cleanup/asterisk_
      # reconciliation need a single leader, and they already have it.
      # replicas: 2 below is safe as-is.
      initContainers:
        - name: migrate
          image: autodialer/backend:latest  # build from Dockerfile and push first
          command: ["alembic", "upgrade", "head"]
          envFrom:
            - configMapRef:
                name: autodialer-config
            - secretRef:
                name: autodialer-secrets
      containers:
        - name: backend
          image: autodialer/backend:latest
          ports:
            - containerPort: 8000
          envFrom:
            - configMapRef:
                name: autodialer-config
            - secretRef:
                name: autodialer-secrets
          readinessProbe:
            httpGet:
              path: /api/health/ready
              port: 8000
            initialDelaySeconds: 10
            periodSeconds: 10
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /api/health/live
              port: 8000
            initialDelaySeconds: 15
            periodSeconds: 20
            failureThreshold: 3
          volumeMounts:
            - name: tts-audio
              mountPath: /var/lib/asterisk/sounds/tts
            - name: call-recordings
              mountPath: /var/spool/asterisk/monitor
            - name: backend-logs
              mountPath: /opt/autodialer/logs
            - name: backend-data
              mountPath: /opt/autodialer/data
          resources:
            requests:
              cpu: "250m"
              memory: "384Mi"
            limits:
              memory: "768Mi"
      volumes:
        - name: tts-audio
          persistentVolumeClaim:
            claimName: tts-audio
        - name: call-recordings
          persistentVolumeClaim:
            claimName: call-recordings
        - name: backend-logs
          emptyDir: {}
        - name: backend-data
          persistentVolumeClaim:
            claimName: backend-data
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: backend-data
  namespace: autodialer
spec:
  accessModes: ["ReadWriteOnce"]
  resources:
    requests:
      storage: 5Gi
---
apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: autodialer
spec:
  selector:
    app: backend
  ports:
    - port: 8000
      targetPort: 8000
